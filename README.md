# CIS-3296 - Number Game

## What it does
Takes a count (D) and a starting value (I), prints a short
sequence, and reports the final score. Repeats until you
enter a number less than 2.

## How to run
python3 game_git.py

## Further improvement
Replace the hardcoded values with user input and add validation so non-numeric or empty input doesn't crash.